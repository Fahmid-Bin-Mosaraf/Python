age: int = 25 

def greeting(name: str) -> str: 
    return f"Hello, {name}!" 

print(greeting("Fahmid")) 


# Type hints are added using the colon (:) syntax for variables and the -> syntax for function return types. 